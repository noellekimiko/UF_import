{% extends "@admin/pages/users.html.twig" %}

{% block body_matter %}
    <div class="row">
        <div class="col-md-12">
            <div id="widget-users" class="box box-primary">
                <div class="box-header">
                    <h3 class="box-title pull-left"><i class="fa fa-fw fa-user"></i> {{translate('USER', 2)}}</h3>
                    {% include "tables/table-tool-menu.html.twig" %}
                </div>
                <div class="box-body">
                    {% include "tables/users.html.twig" with {
                            "table" : {
                                "id" : "table-users",
                                "columns" : ["last_activity"]
                            }
                        }
                    %}
                </div>
                {% if checkAccess('create_user') %}
                <div class="box-footer">
                    <button type="button" class="btn btn-success js-user-create">
                        <i class="fa fa-plus-square"></i>  {{ translate("USER.CREATE")}}
                    </button>
                    <button type="button" class="btn btn-success js-user-import">
                        <i class="fa fa-plus-square"></i>  {{ translate("USER.IMPORT.TXT")}}
                    </button>
                </div>
                {% endif %}
            </div>
        </div>
    </div>
{% endblock %}
{% block scripts_page %}
    <!-- Include validation rules -->
    <script>
    {% include "pages/partials/page.js.twig" %}
    </script>

    <!-- Include form widgets JS -->
    {{ assets.js('js/form-widgets') | raw }}

    <!-- Include page-specific JS -->
    {{ assets.js('js/pages/users') | raw }}
    {{ assets.js('js/pages/import') | raw }}
{% endblock %}